meta {
  name: POST /new
  type: http
  seq: 3
}

post {
  url: {{baseurlapi}}{{prefixapi}}/folder/new
  body: json
  auth: inherit
}

body:json {
  {
    "enc_string": "#ENCODED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
