meta {
  name: POST / (all)
  type: http
  seq: 2
}

post {
  url: {{baseurlapi}}{{prefixapi}}/folder
  body: json
  auth: inherit
}

body:json {
  {
    "enc_string":"Perso"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
