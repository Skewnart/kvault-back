meta {
  name: PUT /
  type: http
  seq: 2
}

put {
  url: {{baseurlapi}}{{prefixapi}}/folder/{{id}}
  body: json
  auth: inherit
}

body:json {
  {
    "enc_string": "#ENCODED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
