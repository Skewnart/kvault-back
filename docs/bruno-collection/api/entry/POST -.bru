meta {
  name: POST /
  type: http
  seq: 2
}

post {
  url: {{baseurlapi}}{{prefixapi}}/entry
  body: json
  auth: inherit
}

body:json {
  {
    "enc_string":"#ENCRYPTED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
