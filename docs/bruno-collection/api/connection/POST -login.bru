meta {
  name: POST /login
  type: http
  seq: 2
}

post {
  url: {{baseurlapi}}{{prefixapi}}/connection/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "aze",
    "password": "aze"
  }
}

script:post-response {
  bru.setVar("token",res.body)
}

settings {
  encodeUrl: true
  timeout: 0
}
