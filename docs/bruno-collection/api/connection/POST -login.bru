meta {
  name: POST /login
  type: http
  seq: 1
}

post {
  url: {{baseurlapi}}{{prefixapi}}/connection/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "password"
  }
}

script:post-response {
  bru.setVar("token",res.body)
}

settings {
  encodeUrl: true
  timeout: 0
}
