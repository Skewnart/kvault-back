meta {
  name: POST /invitations
  type: http
  seq: 3
}

post {
  url: {{baseurlapi}}{{prefixapi}}/connection/register/invitations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
