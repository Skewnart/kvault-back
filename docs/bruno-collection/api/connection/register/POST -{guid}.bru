meta {
  name: POST /{guid}
  type: http
  seq: 2
}

post {
  url: {{baseurlapi}}{{prefixapi}}/connection/register/{{guid}}
  body: json
  auth: inherit
}

body:json {
  {
    "username": "user2",
    "password": "test",
    "envelope": {"envelope":"env"},
    "enc_folders": {"enc":""}
  }
}

script:post-response {
  bru.setVar("token",res.body)
}

settings {
  encodeUrl: true
  timeout: 0
}
