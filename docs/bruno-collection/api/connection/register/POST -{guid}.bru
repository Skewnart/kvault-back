meta {
  name: POST /{guid}
  type: http
  seq: 2
}

post {
  url: {{baseurlapi}}{{prefixapi}}/connection/register/{{guid}}
  body: json
  auth: inherit
}

body:json {
  {
    "username": "skewnart",
    "password": "test",
    "envelope": {},
    "enc_folders": "#ENCODED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
